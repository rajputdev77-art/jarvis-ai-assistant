# ── Core brain & voice (Phase 1-3) ──
groq>=0.11.0
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
edge-tts>=6.1.0
pygame>=2.5.0
python-dotenv>=1.0.0
numpy>=1.26.0

# ── Long-term memory (Phase 2) ──
mem0ai>=0.1.40
qdrant-client>=1.9.0
sentence-transformers>=2.7.0

# ── Browser automation (Phase 3 + 4) ──
playwright>=1.42.0
browser-use>=0.1.40
langchain-groq>=0.2.0

# ── Vision + tray (Phase 4 — Goals 2 & 4) ──
Pillow>=10.0.0
pystray>=0.19.5

# ── Phase 5: Generalist powers ──
PyQt6>=6.6.0              # HUD glass overlay
pywin32>=306              # Windows API access
pygetwindow>=0.0.9        # Window enumeration / focus
pyperclip>=1.8.2          # Clipboard read/write
psutil>=5.9.0             # Process / CPU / RAM
google-api-python-client>=2.100.0  # Gmail API
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# ── Phase 7.2: MCP server + multi-lingual voice + tunneling ──
fastmcp>=0.2.0            # MCP server framework — exposes JARVIS to Cursor/Claude Desktop
sarvamai>=0.1.0           # Sarvam Indian-language STT/TTS (Hindi, Tamil, Telugu, etc.)
keyboard>=0.13.5          # Global hotkeys (Win+J)
win11toast>=0.36.0        # Windows toast notifications
python-telegram-bot>=21.6 # Two-way Telegram messaging
openai>=1.0.0             # Used as Ollama compatible client for local fallback

# ── Phase 8: personality config + autostart polish ──
PyYAML>=6.0               # personality.yaml hot-reload

# ── Phase 11: movie-quality voice pipeline ──
faster-whisper>=1.0.0     # Local STT — 5-10x more accurate than Google Speech
elevenlabs>=2.0.0         # Movie-quality TTS (free 10k chars/mo on free tier)
sounddevice>=0.5.0        # Low-latency mic capture
soundfile>=0.13.0         # Audio file I/O
# Plus install cloudflared CLI separately:
# https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
